(()=>{
let CMS=null;
const CMS_URL='../content/site-content.json';
const pick=obj=>{if(obj==null)return'';if(typeof obj==='string')return obj;return obj.ja||obj.en||Object.values(obj)[0]||''};
const esc=s=>String(s??'').replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]));
const attr=s=>esc(s).replace(/`/g,'&#96;');
const dateLabel=s=>String(s||'').replaceAll('-','.');
function ensureComparison(){if(!document.querySelector('.ims-compare')||document.querySelector('script[data-ja-comparison]'))return;const s=document.createElement('script');s.src='../ims-compare-ja.js?v=20260819-234906';s.defer=true;s.dataset.jaComparison='1';document.head.appendChild(s)}
function newsSorted(){return [...(CMS?.news||[])].sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))}
function assetPath(value,fallback){const src=String(value||'').trim();if(!src)return fallback;if(/^(?:https?:)?\/\//.test(src)||src.startsWith('data:')||src.startsWith('/'))return src;return '../'+src.replace(/^\.?\//,'')}
function newsImage(n,cls){if(n?.image)return `<div class="${cls}"><img src="${attr(assetPath(n.image,''))}" alt="${attr(pick(n.title))}" loading="lazy"></div>`;const fallback=assetPath(CMS?.newsFallbackImage,'../assets/news-fallback-user.svg');return `<div class="${cls} cms-image-fallback"><img src="${attr(fallback)}" alt="Baked Kale" loading="lazy"></div>`}
function renderStrip(){const bar=document.querySelector('.news-strip');if(!bar||!CMS)return;const latest=newsSorted()[0];if(!latest){bar.hidden=true;return}bar.hidden=false;const a=bar.querySelector('a'),b=bar.querySelector('b'),spans=bar.querySelectorAll('span');if(a)a.href='news.html';if(b)b.textContent='Latest';if(spans[0])spans[0].textContent=pick(latest.title)||'最新情報'}
function renderNews(){if(!CMS||!document.body.classList.contains('cms-news-page'))return;const all=newsSorted();if(!all.length)return;const featured=all.find(x=>x.featured)||all[0];const lead=document.getElementById('cmsNewsLead');if(lead){lead.href='#';lead.innerHTML=`${newsImage(featured,'cms-lead-image')}<div class="news-lead-copy"><div class="news-label">Top News / ${esc(featured.category||'Update')}</div><h2>${esc(pick(featured.title))}</h2><p>${esc(pick(featured.body))}</p><div class="news-meta">${esc(dateLabel(featured.date))}</div></div>`}const latest=document.getElementById('cmsLatestList');if(latest)latest.innerHTML=all.slice(0,5).map(n=>`<article class="latest-card">${newsImage(n,'cms-latest-image')}<div class="latest-card-copy"><small>${esc(n.category||'Update')} / ${esc(dateLabel(n.date))}</small><strong>${esc(pick(n.title))}</strong></div></article>`).join('');const wire=document.getElementById('cmsNewsWire');if(wire)wire.innerHTML=all.map(n=>`<article class="wire-row"><div class="wire-type">${esc(n.category||'Update')}</div><div class="wire-title">${esc(pick(n.title))}</div><time>${esc(dateLabel(n.date))}</time></article>`).join('');const d=CMS.instagram||{},box=document.getElementById('cmsInstagram');if(box){box.href=d.profileUrl||'#';box.target='_blank';box.rel='noopener';const desc=pick(d.description);const copy=box.querySelector('.instagram-copy');if(copy&&desc&&!copy.querySelector('p'))copy.insertAdjacentHTML('beforeend',`<p>${esc(desc)}</p>`)}}
function render(){renderStrip();renderNews()}
async function load(){try{const r=await fetch(`${CMS_URL}?v=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(r.status);CMS=await r.json();render()}catch(e){console.warn('Japanese CMS content load failed',e);const bar=document.querySelector('.news-strip');if(bar){const spans=bar.querySelectorAll('span');if(spans[0])spans[0].textContent='最新情報'}}}
function init(){ensureComparison();load()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
window.addEventListener('pageshow',load);
})();
